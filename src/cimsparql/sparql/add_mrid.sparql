# Name: Add MRID

PREFIX cim: <${cim}>

INSERT {
  GRAPH ?g {
    ?s cim:IdentifiedObject.mRID ?mrid .
  }
}
WHERE {
  GRAPH ?g {
    ?s a ?rdf_type .
  }
  FILTER NOT EXISTS {
    ?s cim:IdentifiedObject.mRID ?mrid .
  }
  BIND (str(?s) AS ?mrid)
}
